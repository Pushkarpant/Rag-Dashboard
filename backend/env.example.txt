# .env — Copy this, fill in your keys
# NEVER push this file to GitHub

# ── GEMINI (FREE at ai.google.dev) ──────────────────────
GEMINI_API_KEY=your-gemini-api-key-here

# ── PINECONE (Free tier at pinecone.io) ─────────────────
PINECONE_API_KEY=your-pinecone-key-here
PINECONE_INDEX_NAME=rag-documents

# ── AUTH ──────────────────────────────────────────────────
# Generate a real secret with this command, then paste the result below:
#   python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=paste-your-generated-secret-here

# ── HOW TO GET GEMINI KEY ────────────────────────────────
# 1. Go to: ai.google.dev
# 2. Click "Get API Key" → "Create API Key"
# 3. Copy and paste above
# FREE quota: 15 requests/minute, 1500/day

# ── PINECONE INDEX SETTINGS ──────────────────────────────
# Name: rag-documents
# Dimensions: 3072  ← IMPORTANT (Google embeddings = 3072)
# Metric: cosine

# ── NOTE ──────────────────────────────────────────────────
# Users, document ownership, and query logs are stored in a local
# SQLite file (rag_dashboard.db) — created automatically on first run.
# No extra setup needed for this part.
